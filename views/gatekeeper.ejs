<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <title>
        <%= title %>
    </title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
    <style>
        body {
            background: #0a0a14;
        }

        .gatekeeper-page {
            min-height: 100vh;
            display: flex;
            flex-direction: column;
        }

        .gatekeeper-header {
            background: linear-gradient(135deg, #1a1a2e, #16213e);
            padding: 1rem;
            text-align: center;
            border-bottom: 1px solid rgba(255, 255, 255, 0.1);
        }

        .gatekeeper-title {
            font-size: 1.25rem;
            font-weight: 700;
            color: white;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 0.5rem;
        }

        .scanner-container {
            flex: 1;
            display: flex;
            flex-direction: column;
            align-items: center;
            justify-content: center;
            padding: 1rem;
            position: relative;
        }

        .scanner-wrapper {
            position: relative;
            width: 100%;
            max-width: 350px;
            aspect-ratio: 1;
            background: #1a1a2e;
            border-radius: 20px;
            overflow: hidden;
            box-shadow: 0 10px 40px rgba(0, 0, 0, 0.5);
        }

        #video {
            width: 100%;
            height: 100%;
            object-fit: cover;
        }

        .scanner-overlay {
            position: absolute;
            inset: 0;
            border: 3px solid rgba(255, 255, 255, 0.2);
            border-radius: 20px;
            pointer-events: none;
        }

        .scanner-corners {
            position: absolute;
            inset: 20%;
            border: 4px solid #22c55e;
            border-radius: 12px;
            animation: pulse 2s ease-in-out infinite;
        }

        @keyframes pulse {

            0%,
            100% {
                opacity: 1;
                transform: scale(1);
            }

            50% {
                opacity: 0.7;
                transform: scale(0.98);
            }
        }

        .scanner-line {
            position: absolute;
            left: 20%;
            right: 20%;
            height: 3px;
            background: linear-gradient(90deg, transparent, #22c55e, transparent);
            animation: scan 2s ease-in-out infinite;
        }

        @keyframes scan {
            0% {
                top: 20%;
            }

            50% {
                top: 78%;
            }

            100% {
                top: 20%;
            }
        }

        .scanner-status {
            margin-top: 1.5rem;
            text-align: center;
        }

        .status-text {
            font-size: 1rem;
            color: #a0a0b0;
            margin-bottom: 0.5rem;
        }

        .status-text.scanning {
            color: #22c55e;
        }

        /* Manual input fallback */
        .manual-input {
            width: 100%;
            max-width: 350px;
            margin-top: 2rem;
            padding-top: 2rem;
            border-top: 1px solid rgba(255, 255, 255, 0.1);
        }

        .manual-input label {
            display: block;
            color: #a0a0b0;
            font-size: 0.875rem;
            margin-bottom: 0.5rem;
            text-align: center;
        }

        .manual-input-group {
            display: flex;
            gap: 0.5rem;
        }

        .manual-input input {
            flex: 1;
            padding: 0.875rem 1rem;
            border-radius: 12px;
            border: 2px solid rgba(255, 255, 255, 0.1);
            background: rgba(255, 255, 255, 0.05);
            color: white;
            font-size: 1rem;
            font-family: 'Courier New', monospace;
            text-transform: uppercase;
            letter-spacing: 2px;
        }

        .manual-input input:focus {
            outline: none;
            border-color: #22c55e;
        }

        .manual-input button {
            padding: 0.875rem 1.5rem;
            border-radius: 12px;
            background: #22c55e;
            color: white;
            font-weight: 600;
            border: none;
            cursor: pointer;
        }

        /* Result overlay */
        .result-overlay {
            position: fixed;
            inset: 0;
            display: none;
            align-items: center;
            justify-content: center;
            z-index: 1000;
            padding: 2rem;
        }

        .result-overlay.show {
            display: flex;
        }

        .result-overlay.valid {
            background: rgba(34, 197, 94, 0.95);
        }

        .result-overlay.invalid {
            background: rgba(239, 68, 68, 0.95);
        }

        .result-content {
            text-align: center;
            color: white;
        }

        .result-icon {
            font-size: 6rem;
            margin-bottom: 1rem;
        }

        .result-title {
            font-size: 2.5rem;
            font-weight: 800;
            margin-bottom: 1rem;
        }

        .result-message {
            font-size: 1.25rem;
            margin-bottom: 1rem;
            opacity: 0.9;
        }

        .result-details {
            background: rgba(0, 0, 0, 0.2);
            padding: 1rem 1.5rem;
            border-radius: 12px;
            margin-bottom: 1.5rem;
            text-align: left;
        }

        .result-details p {
            margin: 0.25rem 0;
            font-size: 0.875rem;
        }

        .result-close {
            padding: 1rem 2rem;
            background: rgba(255, 255, 255, 0.2);
            border: 2px solid white;
            border-radius: 12px;
            color: white;
            font-size: 1rem;
            font-weight: 600;
            cursor: pointer;
        }

        /* Permissions */
        .permission-prompt {
            text-align: center;
            padding: 2rem;
        }

        .permission-icon {
            font-size: 4rem;
            margin-bottom: 1rem;
        }

        .permission-text {
            color: #a0a0b0;
            margin-bottom: 1.5rem;
        }

        /* ============================================
           MOBILE FULLSCREEN SCANNER MODE
           ============================================ */
        @media (max-width: 767px) {
            .gatekeeper-header {
                padding: 0.5rem;
                position: fixed;
                top: 0;
                left: 0;
                right: 0;
                z-index: 50;
                background: rgba(26, 26, 46, 0.9);
                backdrop-filter: blur(10px);
            }

            .gatekeeper-title {
                font-size: 1rem;
            }

            .scanner-container {
                padding-top: 60px;
                /* Account for fixed header */
                padding-bottom: 0;
            }

            .scanner-wrapper {
                max-width: 100%;
                width: 100%;
                border-radius: 16px;
                aspect-ratio: auto;
                height: 60vh;
                max-height: 400px;
            }

            .manual-input {
                position: fixed;
                bottom: 0;
                left: 0;
                right: 0;
                padding: 1rem;
                padding-bottom: max(1rem, env(safe-area-inset-bottom));
                background: linear-gradient(to top, #0a0a14 0%, rgba(10, 10, 20, 0.98) 100%);
                margin: 0;
                max-width: none;
                border-top: 1px solid rgba(255, 255, 255, 0.1);
                border-radius: 0;
            }

            .manual-input label {
                font-size: 0.75rem;
            }

            .manual-input input {
                font-size: 1rem;
                padding: 0.75rem 1rem;
            }

            .manual-input button {
                min-height: 48px;
                padding: 0.75rem 1.25rem;
            }

            .result-overlay {
                padding: 1rem;
            }

            .result-title {
                font-size: 2rem;
            }

            .result-close {
                min-height: 48px;
                width: 100%;
                max-width: 300px;
            }
        }

        /* Tablet and desktop */
        @media (min-width: 768px) {
            .scanner-wrapper {
                max-width: 400px;
            }
        }
    </style>
</head>

<body>
    <div class="gatekeeper-page">
        <header class="gatekeeper-header">
            <h1 class="gatekeeper-title">
                🔐 Gatekeeper Scanner
            </h1>
        </header>

        <div class="scanner-container">
            <div id="permissionPrompt" class="permission-prompt" style="display: none;">
                <div class="permission-icon">📷</div>
                <h2 style="color: white; margin-bottom: 0.5rem;">Camera Access Required</h2>
                <p class="permission-text">Please allow camera access to scan QR codes</p>
                <button onclick="startScanner()" class="btn btn-primary">Enable Camera</button>
            </div>

            <div id="scannerArea">
                <div class="scanner-wrapper">
                    <video id="video" playsinline autoplay muted></video>
                    <div class="scanner-overlay">
                        <div class="scanner-corners"></div>
                        <div class="scanner-line"></div>
                    </div>
                </div>

                <div class="scanner-status">
                    <p id="statusText" class="status-text scanning">📷 Point camera at QR code</p>
                </div>
            </div>

            <div class="manual-input">
                <label for="manualCode">Or enter ticket code manually:</label>
                <div class="manual-input-group">
                    <input type="text" id="manualCode" placeholder="NEP-XXXXXX" maxlength="12" autocomplete="off">
                    <button onclick="validateManual()">Check</button>
                </div>
            </div>
        </div>

        <!-- Result Overlay -->
        <div id="resultOverlay" class="result-overlay">
            <div class="result-content">
                <div id="resultIcon" class="result-icon"></div>
                <h2 id="resultTitle" class="result-title"></h2>
                <p id="resultMessage" class="result-message"></p>
                <div id="resultDetails" class="result-details"></div>
                <button class="result-close" onclick="closeResult()">Scan Next</button>
            </div>
        </div>
    </div>

    <!-- QR Scanner Library -->
    <script src="https://unpkg.com/@aspect-ratio/qr-scanner@1.4.2/qr-scanner-worker.min.js"></script>
    <script src="/js/scanner.js"></script>
</body>

</html>