<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <title>
        <%= title %>
    </title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
    <style>
        .payment-page {
            min-height: 100vh;
            background: linear-gradient(180deg, #1a1a2e 0%, #16213e 100%);
            padding: 1rem;
        }

        .payment-container {
            max-width: 450px;
            margin: 0 auto;
        }

        .payment-header {
            text-align: center;
            padding: 2rem 0;
        }

        .payment-icon {
            font-size: 3rem;
            margin-bottom: 1rem;
        }

        .payment-title {
            font-size: 1.5rem;
            font-weight: 700;
            margin-bottom: 0.5rem;
        }

        .payment-subtitle {
            color: #a0a0b0;
            font-size: 0.9rem;
        }

        .booking-summary {
            background: rgba(34, 197, 94, 0.1);
            border: 1px solid rgba(34, 197, 94, 0.3);
            border-radius: 12px;
            padding: 1rem;
            margin-bottom: 1.5rem;
        }

        .booking-code {
            font-family: monospace;
            font-size: 1.25rem;
            font-weight: 700;
            color: #22c55e;
            text-align: center;
            margin-bottom: 0.5rem;
        }

        .booking-match {
            text-align: center;
            font-weight: 600;
        }

        .payment-card {
            background: linear-gradient(145deg, #1e1e35, #252545);
            border-radius: 16px;
            padding: 1.5rem;
            margin-bottom: 1.5rem;
        }

        .step {
            display: flex;
            gap: 1rem;
            margin-bottom: 1.5rem;
        }

        .step:last-child {
            margin-bottom: 0;
        }

        .step-number {
            width: 32px;
            height: 32px;
            background: linear-gradient(135deg, #22c55e, #16a34a);
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-weight: 700;
            font-size: 0.875rem;
            flex-shrink: 0;
        }

        .step-content h4 {
            font-size: 1rem;
            margin-bottom: 0.5rem;
        }

        .step-content p {
            color: #a0a0b0;
            font-size: 0.875rem;
            line-height: 1.5;
        }

        .esewa-box {
            background: rgba(96, 187, 70, 0.1);
            border: 2px solid rgba(96, 187, 70, 0.3);
            border-radius: 12px;
            padding: 1.25rem;
            text-align: center;
            margin: 1rem 0;
        }

        .esewa-label {
            font-size: 0.75rem;
            color: #a0a0b0;
            margin-bottom: 0.5rem;
        }

        .esewa-id {
            font-size: 1.5rem;
            font-weight: 800;
            color: #60bb46;
            font-family: monospace;
            letter-spacing: 2px;
        }

        .amount-box {
            background: rgba(255, 255, 255, 0.05);
            border-radius: 8px;
            padding: 1rem;
            text-align: center;
            margin: 1rem 0;
        }

        .amount-label {
            font-size: 0.75rem;
            color: #a0a0b0;
        }

        .amount-value {
            font-size: 2rem;
            font-weight: 800;
            color: #fff;
        }

        .confirm-form {
            margin-top: 1.5rem;
        }

        .confirm-form label {
            display: block;
            font-size: 0.875rem;
            font-weight: 600;
            margin-bottom: 0.5rem;
            color: #a0a0b0;
        }

        .confirm-form input {
            width: 100%;
            padding: 1rem;
            background: rgba(255, 255, 255, 0.05);
            border: 1px solid rgba(255, 255, 255, 0.1);
            border-radius: 8px;
            color: #fff;
            font-size: 1rem;
            margin-bottom: 1rem;
        }

        .confirm-form input::placeholder {
            color: #666;
        }

        .confirm-form input:focus {
            outline: none;
            border-color: #22c55e;
        }

        .note {
            background: rgba(59, 130, 246, 0.1);
            border: 1px solid rgba(59, 130, 246, 0.3);
            border-radius: 8px;
            padding: 1rem;
            font-size: 0.875rem;
            color: #60a5fa;
            text-align: center;
            margin-top: 1.5rem;
        }
    </style>
</head>

<body>
    <div class="payment-page">
        <div class="payment-container">
            <div class="payment-header">
                <div class="payment-icon">💳</div>
                <h1 class="payment-title">Complete Your Payment</h1>
                <p class="payment-subtitle">Send payment via eSewa to confirm your booking</p>
            </div>

            <div class="booking-summary">
                <div class="booking-code">
                    <%= ticket.bookingCode %>
                </div>
                <div class="booking-match">
                    <%= match.team_home %> vs <%= match.team_away %>
                </div>
            </div>

            <div class="payment-card">
                <div class="step">
                    <div class="step-number">1</div>
                    <div class="step-content">
                        <h4>Open eSewa App</h4>
                        <p>Go to "Send Money" or scan the QR code below</p>
                    </div>
                </div>

                <div class="step">
                    <div class="step-number">2</div>
                    <div class="step-content">
                        <h4>Send to this eSewa ID</h4>
                        <div class="esewa-box">
                            <div class="esewa-label">eSewa Mobile Number</div>
                            <div class="esewa-id">
                                <%= esewaId %>
                            </div>
                        </div>
                    </div>
                </div>

                <div class="step">
                    <div class="step-number">3</div>
                    <div class="step-content">
                        <h4>Send Exact Amount</h4>
                        <div class="amount-box">
                            <div class="amount-label">Amount to Pay</div>
                            <div class="amount-value">Rs. <%= ticket.totalAmount.toLocaleString() %>
                            </div>
                        </div>
                        <p style="margin-top: 0.5rem;">
                            <%= ticket.quantity %> ticket<%= ticket.quantity> 1 ? 's' : '' %> × Rs. <%=
                                        (ticket.totalAmount / ticket.quantity).toLocaleString() %>
                        </p>
                    </div>
                </div>

                <div class="step">
                    <div class="step-number">4</div>
                    <div class="step-content">
                        <h4>Enter Transaction ID</h4>
                        <p>After payment, eSewa shows a reference/transaction ID. Enter it below.</p>

                        <form action="/confirm_payment" method="POST" class="confirm-form">
                            <input type="hidden" name="ticket_id" value="<%= ticket.id %>">
                            <label for="esewa_ref">eSewa Transaction ID *</label>
                            <input type="text" id="esewa_ref" name="esewa_ref" required
                                placeholder="e.g., 0007ABC123 or ref number from eSewa">
                            <button type="submit" class="btn btn-primary btn-block">
                                ✓ I've Completed Payment
                            </button>
                        </form>
                    </div>
                </div>
            </div>

            <div class="note">
                ⏱️ Your booking is held for 30 minutes.<br>
                Complete payment to secure your tickets.
            </div>

            <div style="text-align: center; margin-top: 1.5rem;">
                <a href="/" style="color: #60a5fa; font-size: 0.875rem;">Cancel and return home</a>
            </div>
        </div>
    </div>
</body>

</html>