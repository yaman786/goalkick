<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <meta name="description" content="GoalKick Lite - Buy football match tickets in Nepal with eSewa payment">
    <meta name="theme-color" content="#1a1a2e">
    <title>
        <%= title %>
    </title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
</head>

<body>
    <div class="app-container">
        <header class="header">
            <a href="/" class="logo">
                <span class="logo-icon">⚽</span>
                <span class="logo-text">GoalKick <span class="logo-lite">Lite</span></span>
            </a>
            <nav class="nav">
                <a href="/" class="nav-link active">Matches</a>
                <a href="/find-ticket" class="nav-link">Find Ticket</a>
            </nav>
        </header>

        <main class="main-content">
            <style>
                .success-banner {
                    background: linear-gradient(135deg, rgba(34, 197, 94, 0.2), rgba(16, 163, 74, 0.2));
                    border: 1px solid rgba(34, 197, 94, 0.4);
                    border-radius: 12px;
                    padding: 1.5rem;
                    margin-bottom: 1.5rem;
                    text-align: center;
                }

                .success-banner-icon {
                    font-size: 2.5rem;
                    margin-bottom: 0.75rem;
                }

                .success-banner-title {
                    font-size: 1.25rem;
                    font-weight: 700;
                    color: #4ade80;
                    margin-bottom: 0.5rem;
                }

                .success-banner-text {
                    color: #a0a0b0;
                    font-size: 0.9rem;
                    line-height: 1.6;
                }

                .success-banner-ref {
                    font-family: monospace;
                    background: rgba(255, 255, 255, 0.1);
                    padding: 0.5rem 1rem;
                    border-radius: 8px;
                    display: inline-block;
                    margin: 0.75rem 0;
                    font-weight: 600;
                }
            </style>

            <script>
                // Unified Payment Success Handler
                document.addEventListener('DOMContentLoaded', function () {
                    const params = new URLSearchParams(window.location.search);
                    if (params.get('payment') === 'success') {
                        const ref = params.get('ref') || '';

                        // 1. Show Banner
                        const banner = document.getElementById('successBanner');
                        if (banner) {
                            banner.style.display = 'block';
                            const refSpan = document.getElementById('esewaRefDisplay');
                            if (refSpan && ref) refSpan.textContent = ref;
                        }

                        // 2. Show Modal (New enhancement)
                        const modal = document.getElementById('verificationModal');
                        if (modal && ref) {
                            document.getElementById('modalRef').textContent = ref;
                            modal.style.display = 'flex';
                        }

                        // 3. Clean URL (Only after UI is updated)
                        window.history.replaceState({}, document.title, '/');
                    }
                });
            </script>

            <div id="successBanner" class="success-banner" style="display: none;">
                <div class="success-banner-icon">✅</div>
                <div class="success-banner-title">Payment Details Submitted</div>
                <div class="success-banner-text">
                    Your eSewa Transaction ID: <strong style="color: #fff;"><span
                            id="esewaRefDisplay">—</span></strong><br>
                    <span style="display: inline-block; margin-top: 0.5rem;">
                        Wait for verification (approx 5-10 mins).<br>
                        Check "Find Ticket" for status.
                    </span>
                </div>
                <div style="margin-top: 1rem; display: flex; gap: 0.5rem; justify-content: center;">
                    <a href="/find-ticket" class="btn btn-sm btn-primary">Find Ticket</a>
                    <button onclick="document.getElementById('successBanner').style.display='none'"
                        class="btn btn-sm btn-outline">Close</button>
                </div>
            </div>

            <section class="hero">
                <h1>🏆 Nepal Football Tickets</h1>
                <p class="hero-subtitle">Book your match tickets instantly with eSewa payment</p>
                <div style="margin-top: 2rem;">
                    <a href="#matches" class="btn btn-primary"
                        style="padding: 1rem 3rem; font-size: 1.1rem; box-shadow: 0 4px 14px 0 rgba(34, 197, 94, 0.39);">
                        Browse Matches 👇
                    </a>
                </div>
            </section>

            <section class="matches-section" id="matches">
                <h2 class="section-title">Upcoming Matches</h2>

                <% if (matches.length===0) { %>
                    <div class="empty-state">
                        <span class="empty-icon">📅</span>
                        <h3>No upcoming matches</h3>
                        <p>Check back later for new match announcements!</p>
                    </div>
                    <% } else { %>
                        <div class="matches-grid">
                            <% matches.forEach(match=> { %>
                                <div class="match-card">
                                    <div class="match-date-badge">
                                        <%= formatDate(match.match_date) %>
                                    </div>

                                    <div class="match-teams">
                                        <span class="team home">
                                            <%= match.team_home %>
                                        </span>
                                        <span class="vs">VS</span>
                                        <span class="team away">
                                            <%= match.team_away %>
                                        </span>
                                    </div>

                                    <div class="match-venue">
                                        <span class="venue-icon">📍</span>
                                        <%= match.venue %>
                                    </div>

                                    <div class="match-info">
                                        <div class="info-item">
                                            <span class="info-label">Price</span>
                                            <span class="info-value price">
                                                <%= formatPrice(match.price) %>
                                            </span>
                                        </div>
                                        <div class="info-item">
                                            <span class="info-label">Available</span>
                                            <span
                                                class="info-value seats <%= match.available_seats < 50 ? 'low' : '' %>">
                                                <%= match.available_seats %> / <%= match.total_seats %>
                                            </span>
                                        </div>
                                    </div>

                                    <% if (match.available_seats> 0) { %>
                                        <a href="/match/<%= match.id %>" class="btn btn-primary btn-block">
                                            🎟️ Buy Tickets
                                        </a>
                                        <% } else { %>
                                            <button class="btn btn-disabled btn-block" disabled>
                                                Sold Out
                                            </button>
                                            <% } %>
                                </div>
                                <% }) %>
                        </div>
                        <% } %>
            </section>
        </main>

        <footer class="footer">
            <p>© 2025 GoalKick Lite. Built for Nepal 🇳🇵</p>
        </footer>
    </div>
    <!-- Verification Pending Modal -->
    <div id="verificationModal" class="modal-overlay"
        style="display: none; position: fixed; top: 0; left: 0; width: 100%; height: 100%; background: rgba(0,0,0,0.8); z-index: 1000; align-items: center; justify-content: center;">
        <div
            style="background: #1e1e35; padding: 2rem; border-radius: 16px; max-width: 90%; width: 400px; text-align: center; border: 1px solid rgba(255,255,255,0.1);">
            <div style="font-size: 3rem; margin-bottom: 1rem;">⏳</div>
            <h3 style="margin-bottom: 0.5rem;">Verification Pending</h3>
            <p style="color: #a0a0b0; margin-bottom: 1.5rem; font-size: 0.9rem;">
                Your eSewa ID <strong style="color: #f59e0b;" id="modalRef"></strong> is being verified.
                <br>Please wait for admin approval (approx 5-10 mins).
            </p>
            <div style="background: rgba(255,165,0,0.1); padding: 1rem; border-radius: 8px; margin-bottom: 1.5rem;">
                <p style="font-size: 0.8rem; color: #f59e0b;">
                    💡 You can check your status anytime using the "Find Ticket" button.
                </p>
            </div>
            <button onclick="closeModal()" class="btn btn-primary" style="width: 100%;">Okay, I'll Wait</button>
        </div>
    </div>


</body>

</html>