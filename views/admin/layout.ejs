<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>
        <%= title %> | GoalKick Admin
    </title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/admin.css">
</head>

<body class="admin-body">
    <header class="admin-header">
        <a href="/admin" class="admin-logo">
            <span>⚽ GoalKick</span>
            <span>Admin</span>
        </a>

        <nav class="admin-nav">
            <a href="/admin"
                class="admin-nav-link <%= typeof active !== 'undefined' && active === 'dashboard' ? 'active' : '' %>">📊
                Dashboard</a>
            <a href="/admin/matches"
                class="admin-nav-link <%= typeof active !== 'undefined' && active === 'matches' ? 'active' : '' %>">🏟️
                Matches</a>
            <a href="/admin/tickets"
                class="admin-nav-link <%= typeof active !== 'undefined' && active === 'tickets' ? 'active' : '' %>">🎟️
                Tickets</a>
            <a href="/" class="admin-nav-link" target="_blank">🌐 View Site</a>
        </nav>

        <div class="admin-user">
            <% if (typeof admin !=='undefined' && admin) { %>
                👤 <%= admin.name || admin.username %>
                    <a href="/admin/logout" class="btn-admin btn-admin-secondary btn-admin-sm">Logout</a>
                    <% } %>
        </div>
    </header>

    <main class="admin-container">
        <% if (typeof success !=='undefined' && success) { %>
            <div class="alert alert-success">
                <%= success %>
            </div>
            <% } %>
                <% if (typeof error !=='undefined' && error) { %>
                    <div class="alert alert-error">
                        <%= error %>
                    </div>
                    <% } %>

                        <%- body %>
    </main>
</body>

</html>