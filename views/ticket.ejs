<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no">
    <title>
        <%= title %>
    </title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
    <style>
        .ticket-page {
            min-height: 100vh;
            background: linear-gradient(180deg, #1a1a2e 0%, #16213e 100%);
            padding: 2rem 1rem;
        }

        .success-header {
            text-align: center;
            margin-bottom: 2rem;
        }

        .success-icon {
            font-size: 4rem;
            margin-bottom: 1rem;
            animation: bounce 0.6s ease-out;
        }

        @keyframes bounce {

            0%,
            20%,
            50%,
            80%,
            100% {
                transform: translateY(0);
            }

            40% {
                transform: translateY(-20px);
            }

            60% {
                transform: translateY(-10px);
            }
        }

        .success-title {
            font-size: 1.75rem;
            font-weight: 800;
            color: #22c55e;
            margin-bottom: 0.5rem;
        }

        .success-subtitle {
            color: #a0a0b0;
        }

        .ticket-card {
            background: linear-gradient(145deg, #1e1e35, #252545);
            border-radius: 20px;
            overflow: hidden;
            max-width: 400px;
            margin: 0 auto 2rem;
            box-shadow: 0 20px 40px rgba(0, 0, 0, 0.4);
        }

        .ticket-header {
            background: linear-gradient(135deg, #22c55e, #16a34a);
            padding: 1.5rem;
            text-align: center;
        }

        .ticket-header h2 {
            color: white;
            font-size: 1.25rem;
            font-weight: 700;
            margin: 0;
        }

        .ticket-header .ticket-code {
            font-family: 'Courier New', monospace;
            font-size: 1.5rem;
            font-weight: 800;
            color: white;
            background: rgba(0, 0, 0, 0.2);
            padding: 0.5rem 1rem;
            border-radius: 8px;
            margin-top: 0.75rem;
            display: inline-block;
            letter-spacing: 2px;
        }

        .ticket-body {
            padding: 1.5rem;
        }

        .qr-container {
            background: white;
            padding: 1rem;
            border-radius: 16px;
            text-align: center;
            margin-bottom: 1.5rem;
        }

        .qr-container img {
            max-width: 200px;
            width: 100%;
            height: auto;
        }

        .qr-instruction {
            font-size: 0.75rem;
            color: #666;
            margin-top: 0.5rem;
        }

        .ticket-details {
            border-top: 2px dashed rgba(255, 255, 255, 0.1);
            padding-top: 1.5rem;
        }

        .match-info-ticket {
            text-align: center;
            margin-bottom: 1.5rem;
        }

        .teams-ticket {
            font-size: 1.125rem;
            font-weight: 700;
            color: white;
            margin-bottom: 0.5rem;
        }

        .detail-row {
            display: flex;
            justify-content: space-between;
            padding: 0.75rem 0;
            border-bottom: 1px solid rgba(255, 255, 255, 0.05);
        }

        .detail-row:last-child {
            border-bottom: none;
        }

        .detail-label {
            color: #a0a0b0;
            font-size: 0.875rem;
        }

        .detail-value {
            color: white;
            font-weight: 600;
            font-size: 0.875rem;
        }

        .ticket-footer {
            background: rgba(0, 0, 0, 0.2);
            padding: 1rem 1.5rem;
            text-align: center;
        }

        .ticket-footer p {
            font-size: 0.75rem;
            color: #a0a0b0;
            margin: 0;
        }

        .actions {
            max-width: 400px;
            margin: 0 auto;
            display: flex;
            flex-direction: column;
            gap: 1rem;
        }

        .btn-download {
            background: linear-gradient(135deg, #3b82f6, #2563eb);
        }

        .alert-info {
            background: rgba(59, 130, 246, 0.1);
            border: 1px solid rgba(59, 130, 246, 0.3);
            border-radius: 12px;
            padding: 1rem;
            text-align: center;
            color: #60a5fa;
            font-size: 0.875rem;
            max-width: 400px;
            margin: 0 auto 2rem;
        }
    </style>
</head>

<body>
    <div class="ticket-page">
        <div class="success-header">
            <div class="success-icon">✅</div>
            <h1 class="success-title">
                <% if (alreadyPaid) { %>
                    Your Ticket
                    <% } else { %>
                        Payment Successful!
                        <% } %>
            </h1>
            <p class="success-subtitle">
                <% if (alreadyPaid) { %>
                    Show this QR code at the gate
                    <% } else { %>
                        Your ticket is ready! Save or screenshot this page.
                        <% } %>
            </p>
        </div>

        <% if (!alreadyPaid && typeof referenceId !=='undefined' ) { %>
            <div class="alert-info">
                eSewa Reference: <strong>
                    <%= referenceId %>
                </strong>
            </div>
            <% } %>

                <div class="ticket-card">
                    <div class="ticket-header">
                        <h2>🎫 Entry Ticket</h2>
                        <div class="ticket-code">
                            <%= ticket.qr_code %>
                        </div>
                    </div>

                    <div class="ticket-body">
                        <div class="qr-container">
                            <img src="<%= qrCode %>" alt="Ticket QR Code">
                            <p class="qr-instruction">Show this QR code at the stadium gate</p>
                        </div>

                        <div class="ticket-details">
                            <div class="match-info-ticket">
                                <div class="teams-ticket">
                                    <%= ticket.team_home %> vs <%= ticket.team_away %>
                                </div>
                            </div>

                            <div class="detail-row">
                                <span class="detail-label">📅 Date & Time</span>
                                <span class="detail-value">
                                    <%= new Date(ticket.match_date).toLocaleDateString('en-NP', { weekday: 'short' ,
                                        month: 'short' , day: 'numeric' , hour: '2-digit' , minute: '2-digit' }) %>
                                </span>
                            </div>

                            <div class="detail-row">
                                <span class="detail-label">📍 Venue</span>
                                <span class="detail-value">
                                    <%= ticket.venue %>
                                </span>
                            </div>

                            <div class="detail-row">
                                <span class="detail-label">🎟️ Quantity</span>
                                <span class="detail-value">
                                    <%= ticket.quantity %> ticket<%= ticket.quantity> 1 ? 's' : '' %>
                                </span>
                            </div>

                            <div class="detail-row">
                                <span class="detail-label">💰 Amount Paid</span>
                                <span class="detail-value">Rs. <%= parseFloat(ticket.total_amount).toLocaleString() %>
                                </span>
                            </div>

                            <% if (ticket.user_name) { %>
                                <div class="detail-row">
                                    <span class="detail-label">👤 Name</span>
                                    <span class="detail-value">
                                        <%= ticket.user_name %>
                                    </span>
                                </div>
                                <% } %>
                        </div>
                    </div>

                    <div class="ticket-footer">
                        <p>⚠️ This ticket is valid for ONE entry only.</p>
                    </div>
                </div>

                <div class="actions">
                    <button onclick="downloadTicket()" class="btn btn-primary btn-block btn-download"
                        style="background: linear-gradient(135deg, #3b82f6, #2563eb); margin-bottom: 1rem;">
                        📥 Download Ticket
                    </button>
                    <a href="/" class="btn btn-outline btn-block">← Back to Matches</a>
                </div>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/html2canvas@1.4.1/dist/html2canvas.min.js"></script>
    <script>
        async function downloadTicket() {
            const ticketCard = document.querySelector('.ticket-card');
            if (!ticketCard) return;

            try {
                // Show loading
                const btn = document.querySelector('.btn-download');
                const originalText = btn.innerHTML;
                btn.innerHTML = '⏳ Generating...';
                btn.disabled = true;

                // Wait for QR image to load
                const qrImg = document.querySelector('.qr-container img');
                if (qrImg && !qrImg.complete) {
                    await new Promise(resolve => {
                        qrImg.onload = resolve;
                        setTimeout(resolve, 2000); // Timeout fallback
                    });
                }

                // Generate canvas
                const canvas = await html2canvas(ticketCard, {
                    backgroundColor: '#1a1a2e',
                    scale: 2,
                    useCORS: true,
                    allowTaint: true
                });

                // Download as PNG
                const link = document.createElement('a');
                link.download = 'goalkick-ticket.png';
                link.href = canvas.toDataURL('image/png');
                link.click();

                btn.innerHTML = originalText;
                btn.disabled = false;
            } catch (error) {
                console.error('Download error:', error);
                alert('Could not download ticket. Please take a screenshot instead.');
            }
        }
    </script>
</body>

</html>